# 08 — Status and limitations

Read this before you plan a launch, a listing, an integration, or an announcement.

<!-- generated:status-banner -->
<!-- Generated by scripts/generate-doc-assets.mjs. Do not edit inside this block. -->

> **RC6 is LIVE AND OPEN ON THREE CHAINS.** RC6 is deployed and PUBLIC on Ethereum (1), Base (8453) and Robinhood Chain (4663): all three accept ordinary, permissionless creator launches. The factory is the same CREATE2 address on all three. RC6 is NOT deployed on BNB Smart Chain (56), which is deferred, and no date is set for it.
> Public creator launch is **open** on Ethereum (1), Base (8453), Robinhood Chain (4663) and **closed** on the other 1 configured chain.
> See [08 — Status and limitations](08-status.md).

<!-- /generated:status-banner -->

## Deployment status: RC6 is live and open on three chains

The generation this documentation describes is **RC6**. As of **2026-08-19** it is deployed on
**Ethereum (1)**, **Base (8453)** and **Robinhood Chain (4663)**; on all three the factory holds
`PUBLIC`, so all three accept **ordinary, permissionless creator launches**. Every RC6 contract is
source-verified on its chain's own explorer.

It is **not deployed on BNB Smart Chain (56)**. That chain is configured — it has a chain profile,
the bundle format accepts it, the kit validates against it — and configured is not deployed. **No
date is set for it**, and nothing on this page should be read as one.

### One factory address, three chains

`0x25003C3EBC2036CfE9E4037d4e7E6F840a06522E` is the factory on Ethereum, on Base and on Robinhood
Chain. That is not a transcription error in this documentation: the deployment is deterministic, so
the same creation inputs land at the same address on every chain. Check it on all three explorers
rather than assuming one of them is wrong.

It is an **ERC-1967 proxy**. The proxy address is the stable one, and it is what every launch is
built against; the implementation behind it can be replaced by the upgrade authority, and moves
under it. [11 — Governance and upgradeability](11-governance-and-upgradeability.md) is the whole of
that subject.

### Read the access state yourself

Two facts, and they are answered by different evidence:

- **Whether a chain accepts your launch** is a property of that chain's factory. You can read it
  directly: `launchAccessState()` returns `1` for `PUBLIC` and `0` for `PREPARED`. `PREPARED` is the
  value a freshly initialized factory is born in; `PUBLIC` is reached exactly once, by
  `openPublicLaunches()`, and nothing returns it to `PREPARED`.
- **Whether this repository prints an address for it** is a publication decision, governed by one
  rule — *an address is published only from a source that states it was read back off the chain* —
  and enforced by one command, `npm run kit:deployments:sync`. Nobody hand-types an address into
  this repository, including now.

Both now say the same thing for chains 1, 8453 and 4663, and both say nothing for chain 56.

`relics status` prints all of it in one screen, per generation and per chain, including the chains
nothing is deployed on. It is generated from the same records this page is, so the two cannot drift.

### Something has already launched through it

Robinhood Chain carries **one** project: `666`, token
`0x0C3176ae00ddCDF61c6dC0126208920D6f2Ab9BB`, launched by an ordinary externally-owned account
through `launchWithQuote`. It is a real permissionless launch and not a deployer canary — the
factory reports `canariesUsed` 0. `totalLaunches()` reads 1 on 4663 and 0 on both Ethereum and Base.

If you are holding addresses from an earlier version of this documentation, check them against
`relics status`. **RC5's platform addresses are no longer published in this kit.** RC5 is the
previous generation: its contracts are real and still hold code on the three chains they were
deployed to, and they still read `PREPARED`, which means they refused an ordinary creator launch for
their whole life and refuse one now. An address you can reach and can never use is the worst of the
three options, so the kit prints none of them.

## The chains

<!-- generated:chains -->
<!-- Generated by scripts/generate-doc-assets.mjs. Do not edit inside this block. -->

| Chain | Chain ID | RC6 launch status | Default quote | Additional quote support | Explorer |
| --- | ---: | --- | --- | --- | --- |
| Ethereum | 1 | `DEPLOYED` · public launch open | WETH | None. A non-WETH quote is refused by chain id, not by convention. | `etherscan.io` |
| Base | 8453 | `DEPLOYED` · public launch open | WETH | None. A non-WETH quote is refused by chain id, not by convention. | `basescan.org` |
| Robinhood Chain | 4663 | `DEPLOYED` · public launch open | WETH | Multi-quote. Any asset QuoteAssetRegistryV1 currently admits AND leaves enabled: the stablecoin, the Robinhood stock/ETF tokens and the ecosystem token. Governance admits; a bundle can only request. | `robinhoodchain.blockscout.com` |
| BNB Smart Chain | 56 | `PREPARED_NOT_DEPLOYED` · public launch closed | WBNB | None. WBNB is not WETH; never print "WETH" or "ETH" on this chain. | `bscscan.com` |

RC6 is live and open to ordinary creator launches on Ethereum (1), Base (8453) and Robinhood Chain (4663) -- one factory address, the same on all three, because the deployment is deterministic. BNB Smart Chain (56) is configured in the bundle format and the chain profiles and RC6 is not deployed on it; configured is not deployed, and no date is announced. The chains still differ in the quote leg -- Robinhood is the only multi-quote chain, and chain 56 settles in WBNB rather than WETH -- so read the row rather than assuming they match.

<!-- /generated:chains -->

Two things that table is deliberately not saying.

**The chains are no longer interchangeable, and `PUBLIC` is one-way.** A factory starts in
`PREPARED` — its zero value, so it is closed from the block it is initialized — and moves to
`PUBLIC` exactly once, when the upgrade authority calls `openPublicLaunches()`. Nothing returns it
to `PREPARED`, and when it opens, the pre-public canary entrypoint stops working entirely, so no
privileged creation path outlives the opening. Ethereum, Base and Robinhood Chain have all made
that transition, on 2026-08-19 and 2026-08-20. Chain 56 has no factory to make it with.

**BNB is configured, and configured is not deployed.** Chain 56 carries a full chain profile — WBNB
as the wrapped native, its own gas budget, its own buy-and-entomb accumulation design — and no RC6
contracts. Presence in the schema, the kit, the SDK and the chain config is not a deployment, and no
reader-facing surface may claim the chain-56 buy-and-entomb half is active, converting, or settled.

On chain 56, never print "WETH" or "ETH". The settlement token is WBNB and gas is paid in BNB. The
symbol comes from the chain's own profile, through an accessor that **throws on an unknown chain
rather than returning a default** — because that default was correct on every chain that existed
when it was written and silently wrong on the first one that followed.

## The kit and the deployed product are both RC6

This used to be the sharpest edge in the repository: the kit mirrored RC5's parameter surface while
this documentation described RC6. They agree now, and the distinction that made them able to
disagree is still worth keeping in view.

- **A `.relics` bundle is a description of a project, not of a protocol release.** Its manifest,
  digests, art binding and market mapping are the same artifact across a generation change. That is
  what made a bundle built before RC6 shipped real work rather than wasted work.
- **A bundle never states which contracts are deployed at which addresses.** `runtimeCodeHash` and
  `scriptPointer` are chain facts and are always `null` in a bundle, refused by name if filled in.
  That is exactly what keeps a bundle valid across a generation change.
- **`PROTOCOL_RELEASE_COMPATIBILITY` answers "which parameter surface", not "what is deployed".**
  It reads `v4-art-launchpad/rc6`. What is deployed, where, and whether it accepts your launch is
  answered by `relics status` and by the chain, and those are separate questions on purpose.
- **The kit's exported ADDRESS table is RC6, and RC5's addresses have been withdrawn from it.**
  `relics status` still reports both generations, because a reader who meets an RC5 address
  somewhere else needs to be able to identify it — but the RC5 rows print no address, and say why.
- **Where the two generations differ in behaviour, this documentation is the RC6 answer.** The hook
  permission mask, the dynamic pool fee, the launch-protection election, the disabled fixed-price
  mode, and metadata-at-birth are all RC6 behaviour and all differ from RC5.

The version surface and the address record are updated together, by the sync procedure below.

## How to verify rather than trust

There is a deployed platform on three chains now, and you should verify it rather than take this
page's word. These are the checks that mean something — each one a fact you can reproduce, rather
than a posture somebody asserted:

- **Source verification on the chain's own explorer**, for every deployed contract. Read the runtime
  bytecode against the published source before relying on any description of it, including this page.
- **Runtime codehashes**, compared against the release artifact. A codehash match is checkable; a
  description is not.
- **The hook's address bits.** `address & 0x3FFF` must equal the mask in
  [02 — What a launch produces](02-what-a-launch-produces.md). A hook at an address that does not
  encode its permissions cannot hold those permissions — Uniswap v4 will not call it.
- **The launch-access state**, read from the factory rather than from a document.
- **The immutability of the components you are relying on**, read from the EIP-1967 implementation
  slot: a proxy has one and a directly-deployed contract does not. See
  [11 — Governance and upgradeability](11-governance-and-upgradeability.md), which names which is
  which.

Internal fork work is real evidence about the conditions it tested — the launch path, fee
accounting, art binding and multi-quote settlement have been exercised against pinned forks. That is
a statement about those conditions and nothing wider.

## Known limits worth planning around

- **Three chains, not four.** You can launch on Ethereum (1), Base (8453) and Robinhood Chain
  (4663). On BNB Smart Chain (56) there is no RC6 factory to launch through, and building,
  validating and exporting a bundle is as far as you can go there.
- **Robinhood Chain is the only multi-quote chain.** Ethereum and Base refuse a non-WETH quote by
  chain id, not by convention, so a bundle that elects a stock-token quote is a Robinhood bundle.
- **One transaction, one gas budget.** A launch is a single atomic call and must fit under the
  chain's per-transaction gas cap. That is what forces the script byte budget in
  [04 — Constraints](04-constraints.md). Per-chain budgets must be measured independently rather
  than assumed from Ethereum's numbers.
- **The pool fee is dynamic, and the hook is the policy.** It is not a static tier you pick, and it
  is not a number an interface may print without reading. See
  [06 — Fees and revenue](06-fees-and-revenue.md) and
  [12 — Launch protection](12-launch-protection.md).
- **Fixed-price sale is withdrawn.** It is not "coming soon". The creator kit refuses a bundle that
  elects it at `relics validate` and `relics export`, and the deployed sale contract refuses it at
  launch for every caller. See [05 — The creator flow](05-creator-flow.md).
- **Genesis liquidity is single-sided.** There is no bid depth at launch until real buyers add quote
  currency. Nobody is obliged to buy.
- **Quote admission and WETH conversion are separate.** A quote can be enabled for launch before its
  route to WETH is proven; the buy-and-entomb half then remains quote-denominated and visibly
  pending.
- **Not everything is immutable, in either direction.** Your hook, the liquidity locker and the
  liquidity kernel have no upgrade path at all. Most of the protocol around them is upgradeable by a
  2-of-3 Safe with no timelock. Flattening that into one word is wrong whichever word you pick;
  [11 — Governance and upgradeability](11-governance-and-upgradeability.md) is the table.

## How to talk about this honestly

If you are writing about a project you intend to launch here:

- Name the chains. Say "RC6 is live and open to creator launches on Ethereum, Base and Robinhood
  Chain; it is not deployed on BNB Smart Chain" — not "the launchpad is live", which reads as all
  four.
- Do not turn an undeployed chain into a roadmap. There is **no announced date** for BNB Smart
  Chain, and "coming to BNB" is a claim this repository does not support.
- Cite a checkable fact — a verified source page, a runtime codehash, a hook address whose bits you
  can read — rather than a claim about who has or has not reviewed the code.
- Say "75% of collected LP fees" — not "0.75% of volume forever".
- Say "the platform share is denominated in the selected quote asset" — not "everything settles in
  WETH".
- Say "$RELICS bought and sent permanently to an entombment address; circulating supply falls,
  `totalSupply` stays fixed" — not "supply is burned". The deployed token has no burn path and no
  burn event occurs.
- Say "initialized price" for the price you open the pool at, and never call it a floor, a valuation,
  or a market price. Only trades make a market price.
- For launch protection, use the wording in [12 — Launch protection](12-launch-protection.md), which
  lists both the accurate claims and the ones that are false in either mode.

## Post-deployment sync — the procedure, not a promise

When a chain is broadcast, the deployment facts in this repository are **regenerated from the
production manifest**. Nobody hand-edits an address into a markdown table, because the hand-edited
copy is the one that survives the next redeploy.

This procedure has run for chains 1, 8453 and 4663, which is why their addresses appear in the kit.
It has not run for chain 56, because nothing is deployed there to run it against.

The order matters: the machine-readable record moves first, then everything derived from it.

1. **Pin the deploy commit.** The production deployment manifest for each chain carries a
   `release.sourceCommit`. It is the commit the verified bytecode was built from, and every step
   below is meaningless without it.
2. **Regenerate the deployment record** in `packages/project-schema/src/deployments.js` from the
   launchpad's chain profiles (`launchpad/config/chains/<chainId>.json`) — per-chain contract
   addresses, the release id, the freeze commit and the launch-access state. An address is published
   only from a profile that states its addresses were read back off the chain with `eth_call` /
   `eth_getCode`, which is stronger evidence than a broadcast flag: a flag records that a
   transaction was sent, a verification records that the code is there now. Never transcribe an
   address by hand;
   generate the file and diff it.
3. **Update the version surface** in `packages/project-schema/src/version.js`: check
   `PROTOCOL_RELEASE_COMPATIBILITY` still names the release the kit mirrors, and check the doc
   comments around it still describe the right generation. Bump `CREATOR_KIT_VERSION`. Bump
   `SCHEMA_VERSION` **only** if a manifest field changed meaning — a chain profile is not a bundle
   field.
4. **Update `docs/launchpad/protocol-facts.json`**: for each chain, set `rc6Status` and
   `publicCreatorLaunch` from the chain profile and the on-chain `launchAccessState`, and set
   `deployment.status`, `deployment.statusSentence` and `deployment.addressesPublishedHere`. Every
   field carries the source it mirrors — update the source pointer too if it moved.
5. **Regenerate the docs and assets**: `node scripts/generate-doc-assets.mjs`. Every status banner,
   the chain table, the protection table, the launch-mode table, the immutability table and the fee
   curve update together, in one commit, from one edit.
6. **Verify no page drifted**: `node scripts/generate-doc-assets.mjs --check` must pass, and so must
   `npm run kit:economics` and `node scripts/check-links.mjs`.
7. **Re-verify the published claims against the chain, not the manifest.** For each chain: read
   `launchAccessState` from the factory; read `address & 0x3FFF` for a deployed art hook and confirm
   it equals the published mask; read one launched collection's `contractURI()` and confirm it is
   non-empty; confirm the source-verification status on the explorer. A manifest is a plan. The chain
   is the fact.
8. **Only then** update any prose that describes launch availability, and re-run the export manifest
   check.

Step 7 is the one that is tempting to skip and the one that catches a wrong deploy.

## Reporting a problem

If you find a security-relevant issue in anything published in this repository, follow
[`SECURITY.md`](../../SECURITY.md). Do not open a public issue with a working exploit path.
