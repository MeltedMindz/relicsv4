{
  "name": "@relics/agent-flow",
  "version": "1.0.0",
  "description": "Deterministic, resumable orchestration for an autonomous RELICS launch: the state machine, the hash-linked receipt chain, the no-double-launch guard, and the closed next-action contract an external coding agent drives.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@relics/launch-sdk": "1.0.0",
    "viem": "^2.37.15"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
